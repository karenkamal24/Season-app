# شرح شامل لنظام حقيبة السفر (Travel Bag System)

## 📋 نظرة عامة على النظام

نظام **حقيبة السفر** هو نظام لإدارة محتويات حقائب السفر مع تتبع الوزن والحد الأقصى المسموح به. النظام يدعم **لغتين** (العربية والإنجليزية) بالكامل.

---

## 🌍 دعم اللغتين (Bilingual Support)

### الحقول المدعومة باللغتين:

#### 1. **ItemCategory (فئات العناصر)**
- `name_en` - الاسم بالإنجليزية
- `name_ar` - الاسم بالعربية
- `icon` - أيقونة الفئة
- `icon_color` - لون الأيقونة

#### 2. **Item (العناصر)**
- `name_en` - الاسم بالإنجليزية
- `name_ar` - الاسم بالعربية
- `description_en` - الوصف بالإنجليزية
- `description_ar` - الوصف بالعربية
- `default_weight` - الوزن الافتراضي
- `weight_unit` - وحدة الوزن (kg/lb)

#### 3. **TravelBag (حقيبة السفر)**
- `name_en` - اسم الحقيبة بالإنجليزية
- `name_ar` - اسم الحقيبة بالعربية
- `max_weight` - الحد الأقصى للوزن
- `weight_unit` - وحدة الوزن

#### 4. **BagType (نوع الحقيبة)**
- `name_en` - الاسم بالإنجليزية
- `name_ar` - الاسم بالعربية
- `description_en` - الوصف بالإنجليزية
- `description_ar` - الوصف بالعربية

### كيفية استخدام اللغتين في API:

جميع الـ API Responses ترجع البيانات باللغتين:
```json
{
  "name": "Shirt",           // الإنجليزية
  "name_arabic": "قميص",     // العربية
  "category": "Clothing",
  "category_arabic": "ملابس"
}
```

---

## 🏗️ بنية النظام (System Architecture)

### النماذج (Models) والعلاقات:

```
User (المستخدم)
  └── TravelBag (حقيبة السفر)
       ├── BagType (نوع الحقيبة)
       └── BagItem (عناصر الحقيبة)
            └── Item (العنصر)
                 └── ItemCategory (فئة العنصر)
```

### العلاقات التفصيلية:

1. **User → TravelBag** (One-to-Many)
   - كل مستخدم يمكن أن يكون له عدة حقائب
   - حالياً النظام يستخدم حقيبة واحدة فقط (`main_cargo`)

2. **TravelBag → BagType** (Many-to-One)
   - كل حقيبة لها نوع (مثل: حقيبة الشحن الرئيسية، حقيبة اليد)

3. **TravelBag → BagItem** (One-to-Many)
   - كل حقيبة تحتوي على عدة عناصر

4. **BagItem → Item** (Many-to-One)
   - كل عنصر في الحقيبة يشير إلى عنصر في قاعدة البيانات

5. **Item → ItemCategory** (Many-to-One)
   - كل عنصر ينتمي إلى فئة معينة

---

## 📱 السيناريوهات والاستخدامات (Scenarios)

### السيناريو 1: إنشاء/الحصول على حقيبة السفر

**API:** `GET /api/travel-bag/details`

**الوصف:**
- عند طلب تفاصيل الحقيبة، النظام يبحث عن حقيبة من نوع `main_cargo` للمستخدم
- إذا لم توجد، يتم إنشاؤها تلقائياً بقيم افتراضية:
  - `max_weight`: 23.0 kg
  - `weight_unit`: kg
  - `is_active`: true

**الاستجابة:**
```json
{
  "success": true,
  "data": {
    "bag_id": 1,
    "bag_name": "شنطة الشحن الرئيسية",
    "bag_type": "main_cargo",
    "current_weight": 5.5,
    "max_weight": 23.0,
    "weight_unit": "kg",
    "weight_percentage": 23.91,
    "items": [...],
    "is_empty": false
  }
}
```

---

### السيناريو 2: تحديث الحد الأقصى للوزن

**API:** `PUT /api/travel-bag/max-weight`

**الوصف:**
- المستخدم يريد تغيير الحد الأقصى للوزن المسموح به
- يمكن تغيير وحدة الوزن أيضاً (kg أو lb)

**المعاملات:**
```json
{
  "max_weight": 30.0,
  "weight_unit": "kg"  // اختياري
}
```

**الاستجابة:**
```json
{
  "success": true,
  "message": "Maximum weight updated successfully",
  "data": {
    "max_weight": 30.0,
    "current_weight": 5.5,
    "weight_percentage": 18.33
  }
}
```

---

### السيناريو 3: إضافة عنصر إلى الحقيبة

**API:** `POST /api/travel-bag/add-item`

**الوصف:**
- المستخدم يريد إضافة عنصر إلى حقيبته
- إذا كان العنصر موجوداً بالفعل، يتم زيادة الكمية
- يمكن تحديد وزن مخصص للعنصر

**المعاملات:**
```json
{
  "item_id": 5,
  "quantity": 2,           // اختياري (افتراضي: 1)
  "custom_weight": 1.5     // اختياري (يستخدم الوزن الافتراضي إذا لم يُحدد)
}
```

**المنطق:**
1. البحث عن العنصر في قاعدة البيانات
2. التحقق من وجود العنصر في الحقيبة
3. إذا كان موجوداً:
   - زيادة الكمية
   - تحديث الوزن المخصص (إذا تم تحديده)
4. إذا لم يكن موجوداً:
   - إنشاء سجل جديد في `bag_items`

**الاستجابة:**
```json
{
  "success": true,
  "message": "Item added successfully",
  "data": {
    "item_added": {
      "item_id": 5,
      "name": "Shirt",
      "name_arabic": "قميص",
      "category": "Clothing",
      "quantity": 2,
      "weight_per_item": 0.5,
      "total_weight": 1.0
    },
    "updated_bag": {
      "current_weight": 6.5,
      "max_weight": 23.0,
      "weight_percentage": 28.26,
      "total_items": 8
    }
  }
}
```

---

### السيناريو 4: حذف/تقليل عنصر من الحقيبة

**API:** `DELETE /api/travel-bag/items/{item_id}`

**الوصف:**
- المستخدم يريد إزالة عنصر أو تقليل كميته
- يمكن تحديد الكمية المراد إزالتها

**المعاملات (Query Parameters):**
```
?quantity=1  // اختياري (إذا لم يُحدد، يتم حذف العنصر بالكامل)
```

**المنطق:**
1. البحث عن العنصر في الحقيبة
2. إذا كانت الكمية المحددة >= الكمية الموجودة:
   - حذف العنصر بالكامل
3. إذا كانت أقل:
   - تقليل الكمية فقط

**الاستجابة:**
```json
{
  "success": true,
  "message": "Item removed successfully",
  "data": {
    "updated_bag": {
      "current_weight": 5.5,
      "max_weight": 23.0,
      "weight_percentage": 23.91,
      "total_items": 7
    }
  }
}
```

---

### السيناريو 5: عرض جميع العناصر في الحقيبة

**API:** `GET /api/travel-bag/items`

**الوصف:**
- الحصول على قائمة بجميع العناصر الموجودة في الحقيبة
- مع تفاصيل الوزن والكمية لكل عنصر

**الاستجابة:**
```json
{
  "success": true,
  "data": {
    "items": [
      {
        "item_id": 5,
        "name": "Shirt",
        "name_arabic": "قميص",
        "category": "Clothing",
        "category_arabic": "ملابس",
        "quantity": 2,
        "weight_per_item": 0.5,
        "total_weight": 1.0,
        "icon": "shirt-icon.png"
      },
      ...
    ],
    "total_weight": 6.5,
    "total_items": 8
  }
}
```

---

### السيناريو 6: عرض الفئات والعناصر المتاحة

**API 1:** `GET /api/items/categories`

**الوصف:**
- الحصول على جميع الفئات النشطة
- مرتبة حسب `sort_order`

**الاستجابة:**
```json
{
  "success": true,
  "data": {
    "categories": [
      {
        "category_id": 1,
        "name": "Clothing",
        "name_arabic": "ملابس",
        "icon": "clothing-icon.png",
        "icon_color": "#FF5733"
      },
      ...
    ]
  }
}
```

**API 2:** `GET /api/items?category_id={category_id}`

**الوصف:**
- الحصول على جميع العناصر في فئة معينة
- مرتبة حسب `sort_order`

**الاستجابة:**
```json
{
  "success": true,
  "data": {
    "category": {
      "category_id": 1,
      "name": "Clothing",
      "name_arabic": "ملابس"
    },
    "items": [
      {
        "item_id": 5,
        "name": "Shirt",
        "name_arabic": "قميص",
        "default_weight": 0.5,
        "weight_unit": "kg",
        "category_id": 1,
        "icon": "shirt-icon.png",
        "description": "Cotton shirt",
        "description_arabic": "قميص قطني"
      },
      ...
    ]
  }
}
```

---

## ⚙️ الميزات الرئيسية (Key Features)

### 1. حساب الوزن التلقائي (Automatic Weight Calculation)

النظام يحسب الوزن الحالي تلقائياً باستخدام:
- **Accessor** في نموذج `TravelBag`: `getCurrentWeightAttribute()`
- يحسب مجموع: `(custom_weight ?? default_weight) × quantity` لكل عنصر

```php
public function getCurrentWeightAttribute(): float
{
    return $this->bagItems->sum(function ($bagItem) {
        $weight = $bagItem->custom_weight ?? $bagItem->item->default_weight;
        return $weight * $bagItem->quantity;
    });
}
```

### 2. حساب النسبة المئوية للوزن (Weight Percentage)

**Accessor**: `getWeightPercentageAttribute()`
- يحسب نسبة الوزن الحالي من الحد الأقصى
- الصيغة: `(current_weight / max_weight) × 100`

### 3. الوزن المخصص (Custom Weight)

- كل عنصر له `default_weight` في قاعدة البيانات
- يمكن للمستخدم تحديد `custom_weight` مختلف عند الإضافة
- الأولوية: `custom_weight` > `default_weight`

### 4. إدارة الكمية (Quantity Management)

- يمكن إضافة نفس العنصر عدة مرات (يزيد الكمية)
- يمكن تقليل الكمية أو حذف العنصر بالكامل
- الكمية الافتراضية عند الإضافة: 1

---

## 🔐 الأمان والمصادقة (Authentication)

جميع الـ APIs محمية بـ **Sanctum Authentication**:
- يجب إرسال `Authorization: Bearer {token}` في الـ Headers
- المستخدم يمكنه الوصول فقط إلى حقائبه الخاصة

---

## 📊 قاعدة البيانات (Database Structure)

### الجداول الرئيسية:

1. **travel_bags**
   - `id`, `user_id`, `bag_type_id`
   - `name_en`, `name_ar`
   - `max_weight`, `weight_unit`
   - `is_active`

2. **bag_items**
   - `id`, `travel_bag_id`, `item_id`
   - `quantity`, `custom_weight`

3. **items**
   - `id`, `category_id`
   - `name_en`, `name_ar`
   - `default_weight`, `weight_unit`
   - `icon`, `description_en`, `description_ar`
   - `is_active`, `sort_order`

4. **item_categories**
   - `id`
   - `name_en`, `name_ar`
   - `icon`, `icon_color`
   - `sort_order`, `is_active`

5. **bag_types**
   - `id`, `code`
   - `name_en`, `name_ar`
   - `description_en`, `description_ar`
   - `default_max_weight`, `is_active`

---

## 🎯 حالات الاستخدام الشائعة (Common Use Cases)

### 1. المستخدم يبدأ رحلة جديدة
```
1. فتح التطبيق
2. GET /api/travel-bag/details → إنشاء حقيبة جديدة تلقائياً
3. GET /api/items/categories → عرض الفئات
4. GET /api/items?category_id=1 → عرض العناصر
5. POST /api/travel-bag/add-item → إضافة عناصر
```

### 2. المستخدم يريد التحقق من الوزن
```
1. GET /api/travel-bag/details
2. عرض: current_weight, max_weight, weight_percentage
```

### 3. المستخدم يريد تعديل الحد الأقصى للوزن
```
1. PUT /api/travel-bag/max-weight
2. إرسال: { "max_weight": 30.0 }
```

### 4. المستخدم يريد إزالة عنصر
```
1. DELETE /api/travel-bag/items/{item_id}
2. أو: DELETE /api/travel-bag/items/{item_id}?quantity=1
```

---

## 🔄 تدفق البيانات (Data Flow)

### عند إضافة عنصر:
```
User Request
  ↓
TravelBagController::addItem()
  ↓
1. التحقق من صحة البيانات (Validation)
  ↓
2. البحث عن TravelBag أو إنشائها
  ↓
3. البحث عن Item في قاعدة البيانات
  ↓
4. التحقق من وجود BagItem
   ├─ موجود → تحديث الكمية
   └─ غير موجود → إنشاء BagItem جديد
  ↓
5. حساب الوزن الجديد
  ↓
6. إرجاع الاستجابة مع البيانات المحدثة
```

### عند حساب الوزن:
```
TravelBag Model
  ↓
getCurrentWeightAttribute() Accessor
  ↓
1. جلب جميع bagItems مع العلاقات
  ↓
2. لكل bagItem:
   - استخدام custom_weight أو default_weight
   - ضرب في quantity
   - جمع النتائج
  ↓
3. إرجاع المجموع
```

---

## 📝 ملاحظات مهمة (Important Notes)

1. **الحقيبة الافتراضية**: النظام حالياً يستخدم نوع حقيبة واحد فقط (`main_cargo`)
2. **الإنشاء التلقائي**: الحقيبة تُنشأ تلقائياً عند أول استخدام
3. **الوزن الافتراضي**: 23.0 kg للحقيبة الجديدة
4. **الوحدات المدعومة**: kg و lb
5. **الترتيب**: الفئات والعناصر مرتبة حسب `sort_order`
6. **الحالة النشطة**: فقط العناصر والفئات النشطة (`is_active = true`) تظهر في النتائج

---

## 🚀 أمثلة على الاستخدام الكامل

### مثال 1: إعداد حقيبة سفر كاملة

```bash
# 1. الحصول على الحقيبة
GET /api/travel-bag/details

# 2. عرض الفئات
GET /api/items/categories

# 3. عرض عناصر فئة الملابس
GET /api/items?category_id=1

# 4. إضافة قميصين
POST /api/travel-bag/add-item
{
  "item_id": 5,
  "quantity": 2
}

# 5. إضافة بنطلون
POST /api/travel-bag/add-item
{
  "item_id": 6,
  "quantity": 1
}

# 6. عرض جميع العناصر
GET /api/travel-bag/items

# 7. التحقق من الوزن
GET /api/travel-bag/details
```

### مثال 2: تعديل محتويات الحقيبة

```bash
# 1. إزالة قميص واحد
DELETE /api/travel-bag/items/5?quantity=1

# 2. تحديث الحد الأقصى للوزن
PUT /api/travel-bag/max-weight
{
  "max_weight": 30.0,
  "weight_unit": "kg"
}

# 3. إضافة عنصر بوزن مخصص
POST /api/travel-bag/add-item
{
  "item_id": 10,
  "quantity": 1,
  "custom_weight": 2.5
}
```

---

## ✅ الخلاصة

النظام يوفر:
- ✅ إدارة كاملة لحقيبة السفر
- ✅ تتبع الوزن التلقائي
- ✅ دعم لغتين (عربي/إنجليزي)
- ✅ واجهة API واضحة ومنظمة
- ✅ حساب النسبة المئوية للوزن
- ✅ إدارة الكميات
- ✅ وزن مخصص للعناصر

---

**آخر تحديث:** 2024

