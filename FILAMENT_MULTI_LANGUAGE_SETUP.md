# إعداد نظام اللغتين في Filament

تم إعداد نظام اللغتين (العربية والإنجليزية) في لوحة التحكم Filament بنجاح! 🎉

## ما تم عمله:

### 1. إضافة حقل اللغة للمستخدمين
- تم إضافة حقل `locale` إلى جدول المستخدمين
- يحفظ تفضيل لغة كل مستخدم

### 2. إنشاء Middleware للغة
- ملف: `app/Http/Middleware/SetLocale.php`
- يطبق اللغة المحفوظة للمستخدم تلقائياً

### 3. إنشاء Widget لتبديل اللغة
- ملف: `app/Filament/Widgets/LanguageSwitcherWidget.php`
- يظهر في لوحة التحكم أزرار لتبديل اللغة
- يحفظ الاختيار في قاعدة البيانات

### 4. ملفات الترجمة العربية
- ملف: `lang/ar/filament-panels.php`
- ملف: `lang/ar/filament.php`
- تحتوي على ترجمات عربية لواجهة Filament

### 5. دعم RTL (من اليمين لليسار)
- يتم تفعيل RTL تلقائياً عند اختيار العربية
- تم إضافة CSS مخصص في: `resources/css/filament/admin/theme.css`

## كيفية الاستخدام:

### في لوحة التحكم:
1. سجل الدخول إلى لوحة التحكم `/admin`
2. ستجد أزرار "English" و "العربية" في الأعلى
3. اضغط على اللغة المطلوبة
4. ستتغير اللغة فوراً وسيتم حفظ اختيارك

### برمجياً:
```php
// تغيير لغة المستخدم
auth()->user()->update(['locale' => 'ar']);

// الحصول على اللغة الحالية
$locale = auth()->user()->locale;

// تغيير اللغة في الجلسة
session()->put('locale', 'ar');
app()->setLocale('ar');
```

## الملفات المعدلة:

1. **database/migrations/2025_11_12_000000_add_locale_to_users_table.php**
   - Migration لإضافة حقل locale

2. **app/Models/User.php**
   - إضافة `locale` إلى `$fillable`

3. **app/Http/Middleware/SetLocale.php**
   - Middleware لتطبيق اللغة

4. **app/Providers/Filament/AdminPanelProvider.php**
   - إضافة SetLocale middleware
   - إضافة LanguageSwitcherWidget

5. **app/Providers/AppServiceProvider.php**
   - إضافة دعم RTL لـ Filament

6. **app/Filament/Widgets/LanguageSwitcherWidget.php**
   - Widget لتبديل اللغة

7. **resources/views/filament/widgets/language-switcher-widget.blade.php**
   - View للـ widget

8. **lang/ar/filament-panels.php** و **lang/ar/filament.php**
   - ملفات الترجمة العربية

9. **resources/css/filament/admin/theme.css**
   - CSS مخصص لدعم RTL

## اللغات المتاحة:

- **en**: English (الإنجليزية)
- **ar**: العربية (Arabic)

## ملاحظات:

- اللغة الافتراضية هي الإنجليزية
- يتم حفظ اختيار اللغة لكل مستخدم
- اللغة تنطبق على جميع صفحات لوحة التحكم
- الوجهة (LTR/RTL) تتغير تلقائياً حسب اللغة

## إضافة لغات جديدة:

1. أنشئ مجلد جديد في `lang/` (مثلاً: `lang/fr`)
2. أضف ملفات الترجمة
3. حدث `config/app.php` - `available_locales`
4. حدث Widget لإضافة الزر الجديد

## استكشاف الأخطاء:

إذا لم تعمل الترجمة:
```bash
php artisan config:clear
php artisan view:clear
php artisan cache:clear
```

---

✨ تم الإعداد بنجاح! يمكنك الآن استخدام لوحة التحكم بلغتين.

