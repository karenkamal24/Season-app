# 🚀 دليل استخدام Postman للحقائب الذكية

## 📦 الملفات المطلوبة

تم إنشاء ملفين لك:

1. **`Smart_Bags_API_Collection.postman_collection.json`** - مجموعة الطلبات الكاملة
2. **`Smart_Bags_API_Environment.postman_environment.json`** - ملف المتغيرات

---

## 🎯 خطوات التثبيت والاستخدام

### الخطوة 1: تشغيل السيرفر

```bash
php artisan serve
```

السيرفر سيعمل على: `http://localhost:8000`

---

### الخطوة 2: استيراد الملفات إلى Postman

#### أ) استيراد Collection

1. افتح برنامج Postman
2. اضغط على **Import** (في الزاوية العلوية اليسرى)
3. اسحب وأفلت الملف: `Smart_Bags_API_Collection.postman_collection.json`
   - أو اضغط على "Choose Files" واختر الملف
4. اضغط **Import**

#### ب) استيراد Environment

1. في Postman، اضغط على أيقونة **⚙️ Environments** (في الزاوية العلوية اليمنى)
2. اضغط على **Import**
3. اسحب وأفلت الملف: `Smart_Bags_API_Environment.postman_environment.json`
4. اضغط **Import**

#### ج) تفعيل Environment

1. في الزاوية العلوية اليمنى، من القائمة المنسدلة بجوار أيقونة العين 👁️
2. اختر **"Smart Bags API - Environment"**

---

## 🔑 الخطوة 3: تسجيل الدخول والحصول على Token

### 1. افتح طلب Login

```
🔐 Authentication → Login - تسجيل الدخول
```

### 2. عدّل البيانات

في قسم **Body**، ضع بيانات المستخدم الحقيقية:

```json
{
    "email": "your-email@example.com",
    "password": "your-password"
}
```

### 3. اضغط Send

بعد النجاح، سيتم حفظ الـ **Token** تلقائياً في Environment! ✅

---

## 📋 هيكل المجموعة

### 1️⃣ Item Categories - فئات الأغراض (لا يحتاج مصادقة)
- **Get All Categories** - عرض جميع الفئات المتاحة
- **Get Category by ID** - تفاصيل فئة معينة

### 2️⃣ Authentication - المصادقة
- **Login** - تسجيل الدخول (يحفظ Token تلقائياً)
- **Register** - تسجيل حساب جديد

### 3️⃣ Bags Management - إدارة الحقائب
- **Get All Bags** - عرض جميع الحقائب
- **Get All Bags (Filters)** - فلترة حسب الحالة
- **Get All Bags (Upcoming)** - الرحلات القادمة فقط
- **Get Bag Details** - تفاصيل حقيبة معينة
- **Create Bag (Simple)** - إنشاء حقيبة بسيطة
- **Create Bag (With Items)** - إنشاء حقيبة مع أغراض
- **Update Bag** - تعديل حقيبة
- **Delete Bag** - حذف حقيبة

### 4️⃣ Items Management - إدارة الأغراض
- **Add Item (Minimal)** - إضافة غرض (اسم + وزن + فئة فقط)
- **Add Item (Full)** - إضافة غرض مع جميع التفاصيل
- **Update Item** - تعديل غرض
- **Toggle Packed** - تبديل حالة التعبئة
- **Delete Item** - حذف غرض

### 5️⃣ AI Features - الذكاء الاصطناعي
- **Analyze Bag** - تحليل ذكي للحقيبة
- **Get Latest Analysis** - عرض آخر تحليل

### 6️⃣ Complete Test Scenario - سيناريو اختبار كامل
- سيناريو من 6 خطوات (تشغيل بالترتيب)

---

## 🎯 كيفية الاستخدام السريع

### السيناريو الأساسي

#### 0. احصل على الفئات المتاحة (اختياري - لمعرفة الـ IDs)
```
📚 Item Categories → Get All Categories
```
لا يحتاج مصادقة! سيظهر لك:
```json
{
  "data": [
    {"id": 1, "name_ar": "ملابس", "name_en": "Clothing"},
    {"id": 2, "name_ar": "أحذية", "name_en": "Shoes"},
    {"id": 3, "name_ar": "إلكترونيات", "name_en": "Electronics"},
    ...
  ]
}
```

#### 1. تسجيل الدخول
```
🔐 Authentication → Login
```
✅ الـ Token يُحفظ تلقائياً

#### 2. إنشاء حقيبة
```
📦 Bags Management → Create Bag - إنشاء حقيبة بسيطة
```
عدّل البيانات في Body:
```json
{
    "name": "حقيبة رحلة القاهرة",
    "trip_type": "سياحة",
    "duration": 5,
    "destination": "القاهرة",
    "departure_date": "2025-01-15",
    "max_weight": 23
}
```
✅ الـ bag_id يُحفظ تلقائياً

#### 3. إضافة أغراض
```
📝 Items Management → Add Item - إضافة غرض (الحد الأدنى)
```
عدّل البيانات:
```json
{
    "name": "قميص أبيض",
    "weight": 0.3,
    "item_category_id": 1
}
```

**ملاحظة مهمة:** استخدم `item_category_id` (رقم الفئة) وليس النص!

كرر الخطوة لإضافة المزيد من الأغراض.

#### 4. عرض تفاصيل الحقيبة
```
📦 Bags Management → Get Bag Details
```
ستحصل على:
- جميع الأغراض
- الوزن الكلي
- النسبة المئوية
- الوزن المتبقي

#### 5. تحليل ذكي
```
🤖 AI Features → Analyze Bag
```
سيقترح عليك:
- أغراض مهمة نسيتها
- أغراض غير ضرورية
- نصائح لتحسين الحقيبة

---

## 🎬 السيناريو الكامل التلقائي

للاختبار السريع، استخدم المجلد:

```
📊 Complete Test Scenario - سيناريو تجربة كامل
```

قم بتشغيل الطلبات **بالترتيب** من 1️⃣ إلى 6️⃣

كل طلب يحفظ البيانات تلقائياً للطلب التالي! 🚀

---

## 📝 الفئات المتاحة للأغراض

⚠️ **مهم:** استخدم `item_category_id` (الرقم) وليس النص!

للحصول على الفئات: `GET /api/item-categories`

| ID | الفئة بالعربي | الإنجليزي |
|----|--------------|-----------|
| `1` | ملابس | Clothing |
| `2` | أحذية | Shoes |
| `3` | إلكترونيات | Electronics |
| `4` | أدوية وعناية | Medicine & Care |
| `5` | مستندات | Documents |
| `6` | أدوات نظافة | Toiletries |
| `7` | إكسسوارات | Accessories |
| `8` | كتب وترفيه | Books & Entertainment |
| `9` | طعام ووجبات خفيفة | Food & Snacks |
| `10` | أخرى | Other |

---

## 🔧 المتغيرات في Environment

| المتغير | الوصف | يتم ملؤه تلقائياً |
|---------|-------|-------------------|
| `base_url` | عنوان الـ API | يدوياً: `http://localhost:8000/api` |
| `token` | رمز المصادقة | ✅ عند تسجيل الدخول |
| `bag_id` | رقم الحقيبة | ✅ عند إنشاء حقيبة |
| `item_id` | رقم الغرض | ✅ عند إضافة غرض |

---

## 🎯 نصائح مهمة

### ✅ أشياء تحدث تلقائياً:

1. **Token يُحفظ عند Login** ✨
2. **bag_id يُحفظ عند إنشاء حقيبة** ✨
3. **item_id يُحفظ عند إضافة غرض** ✨
4. **الوزن الكلي يُحدّث تلقائياً** ✨

### ⚠️ أشياء يجب عليك فعلها:

1. تأكد من تشغيل السيرفر أولاً
2. قم بتسجيل الدخول للحصول على Token
3. اختر Environment الصحيح من القائمة العلوية

---

## 📸 أمثلة عملية

### مثال 1: إنشاء حقيبة عمل لدبي

```json
POST /api/smart-bags
{
    "name": "حقيبة رحلة دبي",
    "trip_type": "عمل",
    "duration": 4,
    "destination": "دبي",
    "departure_date": "2025-01-20",
    "max_weight": 20
}
```

### مثال 2: إضافة لابتوب

```json
POST /api/smart-bags/1/items
{
    "name": "لابتوب ماك بوك",
    "weight": 2.3,
    "item_category_id": 3,
    "essential": true
}
```

### مثال 3: إضافة ملابس متعددة

```json
POST /api/smart-bags/1/items
{
    "name": "قميص رسمي",
    "weight": 0.3,
    "item_category_id": 1,
    "quantity": 5
}
```

### مثال 4: الحصول على الفئات المتاحة

```bash
GET /api/item-categories
```
لا يحتاج Authorization! 🎉

---

## ❓ حل المشاكل الشائعة

### ❌ خطأ: "Unauthenticated"

**السبب:** لم تقم بتسجيل الدخول أو Token غير صحيح

**الحل:**
1. افتح طلب Login
2. قم بتسجيل الدخول
3. تأكد من اختيار Environment الصحيح

### ❌ خطأ: "bag_id is required"

**السبب:** لم يتم حفظ bag_id في Environment

**الحل:**
1. قم بإنشاء حقيبة جديدة
2. أو ضع رقم الحقيبة يدوياً في Environment

### ❌ خطأ: "Column 'travel_bag_id' not found"

**السبب:** لم تقم بتشغيل Migrations

**الحل:**
```bash
php artisan migrate
```

---

## 🎨 أنواع الرحلات المتاحة

| النوع | الوصف |
|------|-------|
| `عمل` | رحلات العمل والمؤتمرات |
| `سياحة` | رحلات سياحية ترفيهية |
| `عائلية` | رحلات عائلية |
| `علاج` | رحلات علاجية |

---

## 🚀 خطوات البداية السريعة

### 1. شغّل السيرفر
```bash
php artisan serve
```

### 2. افتح Postman

### 3. استورد الملفين:
- `Smart_Bags_API_Collection.postman_collection.json`
- `Smart_Bags_API_Environment.postman_environment.json`

### 4. اختر Environment

### 5. سجل دخول (Login)

### 6. ابدأ الاختبار! 🎉

---

## 📚 موارد إضافية

- **دليل التشغيل السريع:** `SMART_BAGS_QUICK_START_GUIDE.md`
- **التوثيق الكامل:** `API_DOCUMENTATION_SMART_BAGS.md`
- **شرح التطبيق:** `SMART_BAG_IMPLEMENTATION_SUMMARY.md`

---

## 🎯 جاهز للاستخدام!

كل شيء جاهز الآن! 🚀

استمتع باختبار نظام الحقائب الذكية 🎒✈️

**ملاحظة:** جميع الطلبات تدعم اللغة العربية عبر Header:
```
Accept-Language: ar
```

---

**محتاج مساعدة؟** راجع ملف `SMART_BAGS_QUICK_START_GUIDE.md` 📖

