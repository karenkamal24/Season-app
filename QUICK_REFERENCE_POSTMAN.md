# ⚡ مرجع سريع - Postman

## 🚀 البداية (3 أوامر فقط)

```bash
# 1. شغّل السيرفر
php artisan serve

# 2. شغّل البيانات
php artisan db:seed --class=ItemCategorySeeder

# 3. افتح Postman واستورد الملفات
```

---

## 📋 جدول الفئات (احفظه!)

```
1  = ملابس          👔
2  = أحذية          👞
3  = إلكترونيات    💻
4  = أدوية وعناية   💊
5  = مستندات        📄
6  = أدوات نظافة    🧴
7  = إكسسوارات      ⌚
8  = كتب وترفيه     📚
9  = طعام ووجبات    🍔
10 = أخرى           📦
```

---

## ✅ الطريقة الصحيحة

```json
{
    "name": "قميص",
    "weight": 0.3,
    "item_category_id": 1
}
```

## ❌ الطريقة الخاطئة

```json
{
    "name": "قميص",
    "weight": 0.3,
    "category": "ملابس"
}
```

---

## 🎯 أهم 5 طلبات

```
1. 📚 Item Categories → Get All Categories (لا يحتاج Token)
2. 🔐 Authentication → Login (يحفظ Token تلقائياً)
3. 📦 Bags Management → Create Bag
4. 📝 Items Management → Add Item
5. 🤖 AI Features → Analyze Bag
```

---

## 💾 متغيرات تُحفظ تلقائياً

- ✅ `token` ← عند Login
- ✅ `bag_id` ← عند Create Bag
- ✅ `item_id` ← عند Add Item

---

## 🆘 حل سريع للمشاكل

| المشكلة | الحل |
|---------|------|
| Unauthenticated | سجل دخول |
| item_category_id required | استخدم رقم بدلاً من نص |
| الفئة غير موجودة | `php artisan db:seed --class=ItemCategorySeeder` |
| bag_id required | أنشئ حقيبة أولاً |

---

## 📖 للتفاصيل

👉 **`README_POSTMAN_AR.md`** - ابدأ من هنا!

---

**نصيحة:** احفظ جدول الفئات في مكان قريب! 🎯


