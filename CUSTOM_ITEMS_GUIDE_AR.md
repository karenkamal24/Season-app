# دليل استخدام Custom Items (العناصر المخصصة)

## المقدمة

الآن يمكنك إضافة عناصر مخصصة للحقيبة بدون الحاجة لاختيارها من القائمة الموجودة! يمكنك كتابة اسم العنصر والوزن مباشرة.

---

## طريقة الاستخدام

### 1️⃣ إضافة Custom Item (عنصر مخصص)

**Endpoint:** `POST /api/travel-bag/add-item`

**المعاملات المطلوبة:**
- `custom_item_name`: اسم العنصر المخصص (مطلوب)
- `custom_weight`: وزن العنصر بالكيلوجرام (مطلوب للعناصر المخصصة)
- `quantity`: الكمية (اختياري، افتراضي: 1)
- `bag_type_id`: نوع الحقيبة (اختياري، افتراضي: 1)

**مثال Request:**
```json
{
    "custom_item_name": "محمول شخصي",
    "custom_weight": 2.5,
    "quantity": 1,
    "bag_type_id": 1
}
```

**مثال Response:**
```json
{
    "success": true,
    "message": "تمت إضافة العنصر إلى الحقيبة بنجاح",
    "data": {
        "item_added": {
            "item_id": null,
            "custom_item_name": "محمول شخصي",
            "name": "محمول شخصي",
            "category": null,
            "quantity": 1,
            "weight_per_item": 2.5,
            "total_weight": 2.5,
            "icon": null,
            "is_custom": true
        },
        "bag_type_id": 1,
        "bag_name": "الحقيبة الرئيسية",
        "updated_bag": {
            "current_weight": 2.5,
            "max_weight": 25.0,
            "weight_percentage": 10.0,
            "total_items": 1
        }
    }
}
```

---

### 2️⃣ إضافة Regular Item (عنصر عادي)

يمكنك أيضاً إضافة عنصر عادي من القائمة كما هو معتاد:

**مثال Request:**
```json
{
    "item_id": 1,
    "quantity": 2,
    "custom_weight": null,
    "bag_type_id": 1
}
```

---

### 3️⃣ إضافة Custom Item مع كمية أكبر

**مثال Request:**
```json
{
    "custom_item_name": "كتب",
    "custom_weight": 1.5,
    "quantity": 3,
    "bag_type_id": 1
}
```

**النتيجة:**
- الوزن الإجمالي = 1.5 × 3 = 4.5 كجم

---

## القواعد المهمة

### ✅ القواعد الأساسية:

1. **إما `item_id` أو `custom_item_name`:**
   - يجب إرسال إما `item_id` (للعناصر العادية) أو `custom_item_name` (للعناصر المخصصة)
   - لا يمكن إرسال كلاهما في نفس الوقت

2. **`custom_weight` مطلوب للعناصر المخصصة:**
   - عند إرسال `custom_item_name`، يجب إرسال `custom_weight` أيضاً
   - الوزن يجب أن يكون رقم موجب

3. **التكرار:**
   - إذا أضفت نفس الـ custom item مرتين، سيتم زيادة الكمية تلقائياً
   - مثال: إذا أضفت "محمول شخصي" بوزن 2.5 مرتين، الكمية تصبح 2

4. **الحد الأقصى للوزن:**
   - لا يمكن إضافة عناصر إذا كان الوزن الإجمالي سيتجاوز الحد الأقصى للحقيبة

---

## أمثلة للاستخدام في Postman

### مثال 1: إضافة محمول شخصي
```json
{
    "custom_item_name": "محمول شخصي",
    "custom_weight": 2.5
}
```

### مثال 2: إضافة كتب
```json
{
    "custom_item_name": "كتب",
    "custom_weight": 3.0,
    "quantity": 2
}
```

### مثال 3: إضافة أدوات كهربائية
```json
{
    "custom_item_name": "شاحن لابتوب",
    "custom_weight": 0.5,
    "quantity": 1,
    "bag_type_id": 1
}
```

### مثال 4: إضافة ملابس إضافية
```json
{
    "custom_item_name": "جاكيت شتوي",
    "custom_weight": 1.2,
    "quantity": 1
}
```

---

## الاختلافات بين Regular Items و Custom Items

| الميزة | Regular Items | Custom Items |
|--------|---------------|--------------|
| **المصدر** | من جدول `items` | مخصص من المستخدم |
| **الاسم** | من قاعدة البيانات | من المستخدم |
| **الفئة** | لها فئة (category) | بدون فئة |
| **الأيقونة** | لها أيقونة | بدون أيقونة |
| `item_id` | موجود | `null` |
| `custom_item_name` | `null` | موجود |
| `is_custom` | `false` | `true` |

---

## استعراض العناصر

عند استعراض العناصر في الحقيبة (`GET /api/travel-bag/items`)، سترى:

### Regular Item:
```json
{
    "item_id": 1,
    "custom_item_name": null,
    "name": "قميص",
    "category": "ملابس",
    "quantity": 2,
    "weight_per_item": 0.3,
    "total_weight": 0.6,
    "icon": "shirt-icon.png",
    "is_custom": false
}
```

### Custom Item:
```json
{
    "item_id": null,
    "custom_item_name": "محمول شخصي",
    "name": "محمول شخصي",
    "category": null,
    "quantity": 1,
    "weight_per_item": 2.5,
    "total_weight": 2.5,
    "icon": null,
    "is_custom": true
}
```

---

## ملاحظات مهمة

1. **التحديث:**
   - حالياً، لا يمكن تحديث `custom_item_name` بعد الإضافة
   - يمكن تحديث الكمية فقط

2. **الحذف:**
   - يمكن حذف custom items بنفس طريقة حذف regular items
   - `DELETE /api/travel-bag/items/{item_id}` (لكن custom items ليس لها item_id)

3. **الوزن:**
   - الوزن يُحفظ في `custom_weight` دائماً للعناصر المخصصة
   - للعناصر العادية، يمكن استخدام الوزن الافتراضي أو وزن مخصص

---

## الأخطاء المحتملة

### ❌ خطأ: `item_id_or_name_required`
**السبب:** لم ترسل `item_id` أو `custom_item_name`

**الحل:** أرسل إما `item_id` أو `custom_item_name`

### ❌ خطأ: `custom_weight_required_for_custom_item`
**السبب:** أرسلت `custom_item_name` بدون `custom_weight`

**الحل:** أرسل `custom_weight` مع `custom_item_name`

### ❌ خطأ: `cannot_add_more_weight_exceeded`
**السبب:** الوزن الإجمالي سيتجاوز الحد الأقصى للحقيبة

**الحل:** قلل الكمية أو الوزن

---

## الخلاصة

الآن يمكنك:
- ✅ إضافة عناصر مخصصة بالاسم والوزن
- ✅ إضافة عناصر عادية من القائمة
- ✅ خلط بين العناصر العادية والمخصصة في نفس الحقيبة
- ✅ إدارة الكمية والوزن للعناصر المخصصة

**جرب الآن وأضف العناصر المخصصة التي تحتاجها! 🎉**

