# Travel Bag API Documentation
## دليل API شامل لشنطة السفر

---

## 📋 جدول المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [المصادقة](#المصادقة)
3. [أنواع الشنط](#أنواع-الشنط)
4. [Endpoints](#endpoints)
5. [سيناريوهات الاستخدام](#سيناريوهات-الاستخدام)
6. [التذكيرات والإشعارات](#التذكيرات-والإشعارات)
7. [أمثلة كاملة](#أمثلة-كاملة)

---

## نظرة عامة

نظام شنطة السفر يسمح للمستخدمين بـ:
- إدارة شنط سفر متعددة (شنطة رئيسية، شنطة يد، إلخ)
- إضافة عناصر للشنطة (من قاعدة البيانات أو عناصر مخصصة)
- تتبع الوزن والحد الأقصى للوزن
- تحديد موعد السفر والحصول على تذكيرات
- إشعارات تلقائية عند عدم اكتمال الشنطة

---

## المصادقة

جميع الـ endpoints تتطلب مصادقة باستخدام Bearer Token:

```
Authorization: Bearer YOUR_TOKEN
```

---

## أنواع الشنط

النظام يدعم أنواع شنط متعددة:
- **شنطة الشحن الرئيسية** (bag_type_id = 1) - الافتراضي
- **شنطة يد** (bag_type_id = 2)
- أنواع أخرى حسب الإعدادات

---

## Endpoints

### 1. الحصول على تفاصيل الشنطة

**GET** `/api/travel-bag/details`

#### Query Parameters:
- `bag_type_id` (optional): نوع الشنطة. إذا لم يتم تحديده، يتم إرجاع جميع شنط المستخدم

#### Response (شنطة واحدة):
```json
{
  "status": 200,
  "message": "تم جلب بيانات الشنطة بنجاح",
  "data": {
    "bag_id": 1,
    "bag_name": "شنطة الشحن الرئيسية",
    "bag_type": 1,
    "bag_type_id": 1,
    "current_weight": 15.5,
    "max_weight": 23.0,
    "weight_unit": "kg",
    "weight_percentage": 67.39,
    "items": [...],
    "is_empty": false,
    "reminder": {
      "date": "2025-02-15",
      "time": "08:30"
    },
    "status": "ready",
    "is_ready": true
  }
}
```

#### Response (جميع الشنط):
```json
{
  "status": 200,
  "message": "تم جلب بيانات الشنط بنجاح",
  "data": [
    {
      "bag_id": 1,
      "bag_name": "شنطة الشحن الرئيسية",
      ...
    },
    {
      "bag_id": 2,
      "bag_name": "شنطة يد",
      ...
    }
  ]
}
```

---

### 2. تحديث الحد الأقصى للوزن

**PUT** `/api/travel-bag/max-weight`

#### Request Body:
```json
{
  "max_weight": 30.0,
  "weight_unit": "kg",
  "bag_type_id": 1
}
```

#### Response:
```json
{
  "status": 200,
  "message": "تم تحديث الوزن الأقصى بنجاح",
  "data": {
    "max_weight": 30.0,
    "current_weight": 15.5,
    "weight_percentage": 51.67
  }
}
```

---

### 3. إضافة عنصر للشنطة

**POST** `/api/travel-bag/add-item`

#### Request Body (عنصر من قاعدة البيانات):
```json
{
  "item_id": 5,
  "quantity": 2,
  "bag_type_id": 1,
  "custom_weight": null
}
```

#### Request Body (عنصر مخصص):
```json
{
  "custom_item_name": "جهاز إلكتروني",
  "quantity": 1,
  "custom_weight": 1.5,
  "bag_type_id": 1
}
```

#### Validation Rules:
- `item_id`: مطلوب إذا لم يتم تحديد `custom_item_name`
- `custom_item_name`: مطلوب إذا لم يتم تحديد `item_id`
- `custom_weight`: مطلوب إذا تم تحديد `custom_item_name`
- `quantity`: اختياري، افتراضي = 1
- `bag_type_id`: اختياري، افتراضي = 1

#### Response:
```json
{
  "status": 200,
  "message": "تم إضافة العنصر للشنطة بنجاح",
  "data": {
    "item_added": {
      "item_id": 5,
      "name": "قميص",
      "quantity": 2,
      "weight_per_item": 0.3,
      "total_weight": 0.6,
      "is_custom": false
    },
    "bag_type_id": 1,
    "bag_name": "شنطة الشحن الرئيسية",
    "updated_bag": {
      "current_weight": 16.1,
      "max_weight": 23.0,
      "weight_percentage": 70.0,
      "total_items": 8
    }
  }
}
```

#### ملاحظات:
- إذا كان العنصر موجوداً مسبقاً، يتم زيادة الكمية تلقائياً
- النظام يتحقق من عدم تجاوز الوزن الأقصى
- إذا كانت الشنطة غير ممتلئة، يتم إنشاء تذكير يومي تلقائياً

---

### 4. الحصول على عناصر الشنطة

**GET** `/api/travel-bag/items?bag_type_id=1`

#### Response:
```json
{
  "status": 200,
  "message": "تم جلب عناصر الشنطة بنجاح",
  "data": {
    "bag_type_id": 1,
    "bag_name": "شنطة الشحن الرئيسية",
    "items": [
      {
        "item_id": 5,
        "name": "قميص",
        "quantity": 2,
        "weight_per_item": 0.3,
        "total_weight": 0.6,
        "is_custom": false
      },
      {
        "item_id": null,
        "custom_item_name": "جهاز إلكتروني",
        "name": "جهاز إلكتروني",
        "quantity": 1,
        "weight_per_item": 1.5,
        "total_weight": 1.5,
        "is_custom": true
      }
    ],
    "total_weight": 16.1,
    "max_weight": 23.0,
    "weight_percentage": 70.0,
    "total_items": 3
  }
}
```

---

### 5. تحديث كمية عنصر

**PUT** `/api/travel-bag/items/{item_id}/quantity`

#### Request Body:
```json
{
  "quantity": 3,
  "bag_type_id": 1
}
```

#### Response:
```json
{
  "status": 200,
  "message": "تم تحديث الكمية بنجاح",
  "data": {
    "item_updated": {
      "item_id": 5,
      "quantity": 3,
      "total_weight": 0.9
    },
    "updated_bag": {
      "current_weight": 16.4,
      "max_weight": 23.0,
      "weight_percentage": 71.3,
      "total_items": 9
    }
  }
}
```

---

### 6. حذف عنصر من الشنطة

**DELETE** `/api/travel-bag/items/{item_id}?bag_type_id=1&quantity=1`

#### Query Parameters:
- `bag_type_id`: مطلوب
- `quantity`: اختياري - إذا لم يتم تحديده، يتم حذف كل الكمية

#### Response:
```json
{
  "status": 200,
  "message": "تم حذف العنصر بنجاح",
  "data": {
    "bag_type_id": 1,
    "bag_name": "شنطة الشحن الرئيسية",
    "updated_bag": {
      "current_weight": 15.5,
      "max_weight": 23.0,
      "weight_percentage": 67.39,
      "total_items": 7
    }
  }
}
```

---

### 7. تحديد موعد السفر (جديد) ⭐

**POST** `/api/travel-bag/travel-date`

هذا الـ endpoint يختلف عن `setReminder`:
- ✅ يلغي التذكير اليومي تلقائياً
- ✅ يجعل الشنطة `ready` تلقائياً
- ✅ ينشئ تذكير واحد (once) لميعاد السفر
- ✅ التذكير يُرسل فقط عندما تكون الشنطة جاهزة

#### Request Body:
```json
{
  "bag_type_id": 1,
  "date": "2025-02-15",
  "time": "08:30",
  "timezone": "Africa/Cairo"
}
```

#### Response:
```json
{
  "status": 200,
  "message": "تم تحديد موعد السفر بنجاح",
  "data": {
    "bag_type_id": 1,
    "bag_name": "شنطة الشحن الرئيسية",
    "travel_date": "2025-02-15",
    "travel_time": "08:30",
    "reminder": {
      "reminder_id": 10,
      "title": "تذكير برحلتك لهذه الحقيبة",
      "date": "2025-02-15",
      "time": "08:30",
      "recurrence": "once",
      "status": "active"
    },
    "bag_status": "ready",
    "message": "تم تحديد موعد السفر. الشنطة جاهزة وسيتم إرسال التذكير في الموعد المحدد."
  }
}
```

#### ملاحظات مهمة:
- عند استخدام هذا الـ endpoint، الشنطة تصبح `ready` تلقائياً حتى لو لم تكن ممتلئة
- التذكير اليومي ("شنطة السفر لسه مش كاملة") يتم إلغاؤه تلقائياً
- التذكير يُرسل في الموعد المحدد فقط إذا كانت الشنطة جاهزة

---

### 8. الحصول على تذكير الشنطة

**GET** `/api/travel-bag/reminder?bag_type_id=1`

#### Response:
```json
{
  "status": 200,
  "message": "تم جلب تذكير الشنطة بنجاح",
  "data": {
    "bag_type_id": 1,
    "bag_name": "شنطة الشحن الرئيسية",
    "reminders": [
      {
        "reminder_id": 10,
        "title": "تذكير برحلتك لهذه الحقيبة",
        "date": "2025-02-15",
        "time": "08:30",
        "timezone": "Africa/Cairo",
        "recurrence": "once",
        "notes": "رحلتك مجدولة بتاريخ 2025-02-15 الساعة 08:30...",
        "status": "active",
        "last_sent_at": null,
        "created_at": "2025-01-20T10:30:00Z"
      }
    ],
    "has_reminder": true,
    "trip_reminder": {
      "reminder_id": 10,
      ...
    },
    "daily_reminder": null
  }
}
```

#### ملاحظة:
- تذكيرات الشنطة **لا تظهر** في `/api/reminders` (الـ reminders العادية)
- هذا الـ endpoint مخصص فقط لتذكيرات الشنطة

---

### 9. تحديد تذكير سفر (قديم)

**POST** `/api/travel-bag/reminder`

هذا الـ endpoint القديم - يُفضل استخدام `/travel-date` بدلاً منه.

#### Request Body:
```json
{
  "bag_type_id": 1,
  "date": "2025-02-15",
  "time": "08:30",
  "timezone": "Africa/Cairo"
}
```

---

## سيناريوهات الاستخدام

### السيناريو 1: إعداد شنطة جديدة

```bash
# 1. الحصول على تفاصيل الشنطة (يتم إنشاؤها تلقائياً)
GET /api/travel-bag/details?bag_type_id=1

# 2. تحديث الحد الأقصى للوزن
PUT /api/travel-bag/max-weight
{
  "max_weight": 23.0,
  "weight_unit": "kg",
  "bag_type_id": 1
}

# 3. إضافة عناصر
POST /api/travel-bag/add-item
{
  "item_id": 5,
  "quantity": 2,
  "bag_type_id": 1
}
```

---

### السيناريو 2: تحديد موعد السفر

```bash
# 1. تحديد موعد السفر
POST /api/travel-bag/travel-date
{
  "bag_type_id": 1,
  "date": "2025-02-15",
  "time": "08:30",
  "timezone": "Africa/Cairo"
}

# النتيجة:
# - الشنطة تصبح ready تلقائياً
# - التذكير اليومي يتم إلغاؤه
# - يتم إنشاء تذكير لميعاد السفر
```

---

### السيناريو 3: إضافة عناصر مخصصة

```bash
POST /api/travel-bag/add-item
{
  "custom_item_name": "جهاز إلكتروني",
  "quantity": 1,
  "custom_weight": 1.5,
  "bag_type_id": 1
}
```

---

## التذكيرات والإشعارات

### التذكير اليومي (تلقائي)

عند إضافة عناصر للشنطة والشنطة غير ممتلئة:
- يتم إنشاء تذكير يومي تلقائياً
- يُرسل كل يوم الساعة 9 مساءً (21:00)
- الرسالة: "شنطة السفر لسه مش كاملة، لسه في مساحة تضيف حاجات تانية"

**إلغاء التذكير اليومي:**
- عند تحديد موعد سفر (`/travel-date`)
- عند اكتمال الشنطة (الوزن = الحد الأقصى)
- عند إفراغ الشنطة

---

### تذكير موعد السفر

عند تحديد موعد سفر (`/travel-date`):
- يتم إنشاء تذكير واحد (once) لميعاد السفر
- التذكير يُرسل فقط إذا كانت الشنطة `ready`
- إذا كانت الشنطة غير جاهزة، يتم تخطي التذكير (يُسجل في الـ logs)

---

### آلية الإرسال

- الـ scheduler يعمل كل دقيقة (`reminders:send` command)
- يتحقق من التذكيرات النشطة
- يرسل الإشعارات عبر Firebase Cloud Messaging (FCM)
- يتطلب وجود `fcm_token` للمستخدم

---

## أمثلة كاملة

### مثال 1: رحلة كاملة

```bash
# الخطوة 1: الحصول على الشنطة
GET /api/travel-bag/details?bag_type_id=1

# الخطوة 2: تحديث الوزن الأقصى
PUT /api/travel-bag/max-weight
{
  "max_weight": 23.0,
  "bag_type_id": 1
}

# الخطوة 3: إضافة عناصر
POST /api/travel-bag/add-item
{
  "item_id": 1,
  "quantity": 3,
  "bag_type_id": 1
}

POST /api/travel-bag/add-item
{
  "item_id": 2,
  "quantity": 2,
  "bag_type_id": 1
}

POST /api/travel-bag/add-item
{
  "custom_item_name": "جهاز إلكتروني",
  "custom_weight": 1.5,
  "bag_type_id": 1
}

# الخطوة 4: التحقق من العناصر
GET /api/travel-bag/items?bag_type_id=1

# الخطوة 5: تحديد موعد السفر
POST /api/travel-bag/travel-date
{
  "bag_type_id": 1,
  "date": "2025-02-15",
  "time": "08:30"
}

# الخطوة 6: التحقق من التذكير
GET /api/travel-bag/reminder?bag_type_id=1
```

---

### مثال 2: تحديث كمية عنصر

```bash
# تحديث كمية عنصر موجود
PUT /api/travel-bag/items/5/quantity
{
  "quantity": 5,
  "bag_type_id": 1
}
```

---

### مثال 3: حذف عنصر

```bash
# حذف كمية محددة
DELETE /api/travel-bag/items/5?bag_type_id=1&quantity=2

# حذف كل الكمية
DELETE /api/travel-bag/items/5?bag_type_id=1
```

---

## حالات الأخطاء

### خطأ: تجاوز الوزن الأقصى

```json
{
  "status": 500,
  "message": "لا يمكن إضافة المزيد من الوزن - تم تجاوز الحد الأقصى"
}
```

### خطأ: عنصر غير موجود

```json
{
  "status": 404,
  "message": "العنصر غير موجود"
}
```

### خطأ: نوع شنطة غير موجود

```json
{
  "status": 404,
  "message": "نوع الشنطة غير موجود"
}
```

---

## ملاحظات مهمة

1. **الشنطة الافتراضية**: إذا لم يتم تحديد `bag_type_id`، يتم استخدام الشنطة الرئيسية (ID = 1)

2. **الوزن**: يتم حساب الوزن تلقائياً بناءً على:
   - للعناصر العادية: `custom_weight` أو `default_weight` من قاعدة البيانات
   - للعناصر المخصصة: `custom_weight` المقدم

3. **حالة الشنطة**:
   - `ready`: الشنطة جاهزة (إما ممتلئة أو تم تحديد موعد سفر)
   - `not_ready`: الشنطة غير جاهزة

4. **التذكيرات**:
   - التذكير اليومي: يتم إنشاؤه تلقائياً عند إضافة عناصر والشنطة غير ممتلئة
   - تذكير السفر: يتم إنشاؤه عند تحديد موعد سفر
   - تذكيرات الشنطة لا تظهر في `/api/reminders`

5. **الإشعارات**:
   - تتطلب وجود `fcm_token` للمستخدم
   - يتم إرسالها عبر Firebase Cloud Messaging
   - الـ scheduler يعمل كل دقيقة للتحقق من التذكيرات

---

## Headers المطلوبة

```
Authorization: Bearer YOUR_TOKEN
Content-Type: application/json
Accept-Language: ar (أو en)
```

---

## Base URL

```
https://your-api-domain.com/api
```

---

**آخر تحديث**: 2025-01-20

